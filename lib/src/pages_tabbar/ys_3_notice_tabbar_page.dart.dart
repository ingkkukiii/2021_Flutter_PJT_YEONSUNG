import 'package:flutter/material.dart';

class YSungNoticeTabBarPage extends StatelessWidget {
  const YSungNoticeTabBarPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
    );
  }
}
